{
  "name": "2",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node dist/test.js",
    "build": "esbuild test.js --bundle --platform=node --format=esm --packages=external --outdir=dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "aida-sdk": "^1.0.5",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.12"
  }
}
